pcm.!default {
    type plug
    slave.pcm "dmixer"
}

pcm.dmixer {
    type dmix
    ipc_key 1024
    slave {
        pcm "hw:1,0"  # Points to HDA Intel PCH (Analog)
        period_time 0
        period_size 1024
        buffer_size 4096
        rate 44100
    }
}
